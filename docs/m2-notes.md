# M2 작업 노트

> `docs/m2-plan.md`를 실행하면서 생긴 기록을 모은다.
> 규칙의 원본은 `docs/rulebook-v3.md`이며, 이 문서는 구현 과정의 기록일 뿐 규칙을 정의하지 않는다.

## 변경 이력

이전 작업 단위에서 발견한 버그·설계 결함과 그 수정 내역.

| 날짜 | 작업 단위 | 내용 | 이유 |
|---|---|---|---|

## 계획 변경

`docs/m2-plan.md`와 달라진 결정.

| 날짜 | 항목 | 계획 | 실제 | 이유 |
|---|---|---|---|---|
| 2026-09-18 | `PHASE1_COMPLETE` 제거 순서 | M2-1에서 상수를 제거한다 | 상수를 제거하면서 서버의 참조를 `PHASE2_ENTRY`로 함께 바꿨다. 처리기는 M2-4에서 채운다 | M2-1 종료 조건이 shared·server 양쪽 typecheck다. 상수만 지우면 서버가 컴파일되지 않는다. 처리기가 없는 동안 `PHASE2_ENTRY`는 `unhandledStep`으로 거절돼 M1 테스트의 "더 진행하지 않는다"가 그대로 유지된다 |
| 2026-09-18 | 투영 타입의 알림·귓속말 종류 | `NoticeView.kind`, `WhisperView.kind`를 상수 유니온으로 | `string`으로 두었다 | `PUBLIC_NOTICE_KIND`는 서버 상태 모듈, `WHISPER_KIND`는 서버 시나리오 상수다. 이 둘을 shared로 옮기는 것은 M2 계획에 없는 파일 이동이라 하지 않았다. 제안에 남긴다 |
| 2026-09-18 | `progress.slotOrder` | 계획 2.4는 `slotOrder`를 새 필드로 두고 `eventOrder`를 Phase마다 교체한다고 적었다 | 필드를 만들지 않고 `eventOrder`만 교체했다 | 두 필드가 같은 값을 들고 있으면 어긋날 자리가 생긴다. 슬롯 배치는 `eventOrder` 그대로 읽으면 되고, 봇 대전 지표도 여기서 뽑는다 |
| 2026-09-18 | `pickFalseVariant`의 위치 | M1에서는 `rules/whisper.ts`에 있었다 | `rules/variant.ts`로 옮기고 `whisper.ts`가 가져다 쓴다 | 가짜 라벨(§4.3)과 T2 귓속말(§12)이 같은 규칙을 쓰므로 변이 규칙 쪽이 원본이다. 거짓 생성 규칙이 한 곳에 모인다 |
| 2026-09-18 | 좌석 잠식 변경의 단일 진입점 | 계획 4.1은 `applySeatErosion` 한 곳으로 모은다고만 적었다 | `rules/erosion.ts`에 두고 배신자 전환(`traitor.ts`)·봇 방해(`botPolicy.ts`)를 여기서 호출한다 | 잠식 상승·회복이 일어나는 곳(효과·비공개 대가·강제 성공 대가·부적 회복·환경 잠식·진입 환청·14A)이 모두 이 함수를 지나야 100% 검사가 한 곳에 모인다 |
| 2026-09-18 | `seatSetupForHumans`의 하한 | M1은 1~3명만 허용했다 | 0~3명으로 넓혔다 | 봇 자동 대전의 인간 0명 구성이 계획 8절의 상한 참고치다 |
| 2026-09-18 | Phase 2 진입 단계의 시간 | 계획 6절 전이표는 `PHASE2_ENTRY`가 타이머 만료로 첫 이벤트에 간다고 적었다 | 타이머 없이 곧바로 첫 이벤트로 이어진다 | 룰북에 이 연출의 시간이 없다. 없는 설정값을 만들지 않기 위해 즉시 전이로 두었다. §2.1의 이벤트 수에도 세지 않는다 |
| 2026-09-18 | Phase 1 단계 테스트의 잠식 기대값 | — | 이장 이벤트 뒤 Phase 2 진입 환청(+20%)이 같은 처리 안에서 이어지므로 기대값을 함께 적었다 | Phase 1 마지막 이벤트의 결과 적용과 Phase 2 진입이 한 dispatch로 이어진다. 중간 관찰 지점이 없어 테스트가 두 규칙의 합을 본다 |
| 2026-09-18 | `INTERVENTION_FORCE` 단계의 관찰 | 계획 9절 M2-5의 7번은 "Phase 3에서 강제 성공이 거절된다"였다 | 부적 단계에서 명령을 보내 거절을 확인하고, 첫째 단계가 조기 스킵되는지로 검증했다 | Phase 3에서는 `canForceSuccess`가 false라 첫째 단계가 진입 즉시 스킵된다. 그 단계에 머무는 시점이 없어 명령을 보낼 자리가 없다 |
| 2026-09-18 | 엔진·시나리오 버전 문자열 | M1은 `m1` / `phase1-v3` | `m2` / `rulebook-v3` | 상태 구조와 시나리오 범위가 바뀌었다. 세이브 버전 검증(M5)이 이 값으로 갈린다 |

## 확인 필요

룰북에 없어 임의로 정할 수 없는 규칙·수치. 해당 부분에 의존하는 테스트는 `it.todo`로 남긴다.

| 날짜 | 항목 | 왜 필요한가 | 임시 처리 |
|---|---|---|---|
| 2026-09-18 | 봇 100% 방해에서 부적 보유자가 아무도 없을 때 | 룰북 §11은 "다음 중 하나가 랜덤 발동"이라고만 적었다. 부적 소멸이 뽑혔는데 보유자가 없으면 게임당 1회를 빈손으로 소모하는지 알 수 없다 | **임시**: 발동 가능한 수단 중에서 고른다(보유자가 없으면 팀 디버프만 후보). "발동"이 효과가 일어나는 것을 뜻한다고 읽었다 |
| 2026-09-18 | 인간 0명 구성의 Phase 3 A 루트 | 룰북 §14.4의 투표는 인간 플레이어를 전제한다. 봇은 투표하지 않으므로(§11) 인간 0명 구성은 언제나 유효표 0이 되어 강제 잠식으로 끝난다 | **임시**: 룰 그대로 처리한다. 봇 자동 대전의 `--humans 0`은 시계 소모 상한 참고치로만 쓰고, 엔딩 분포는 `--humans 1~3`으로 읽는다 (M2 계획 10.3) |
| 2026-09-18 | 100%(배신자) 좌석의 티어 환청·가짜 라벨·가짜 붉은 메시지 | 룰북 §4.3 표는 60~99% 행에만 이 효과들을 적고 100% 행에는 전환만 적었다. §16은 "30% 이상"이라 두 서술이 겹치는 구간이 있다 | **임시**: §4.3 표를 그대로 읽어 100% 구간은 세 효과 모두 대상에서 제외한다. 배신자는 이미 실제 변이를 전부 보므로(§10.2) 가짜 정보가 의미가 없다 |

## 제안

범위 밖이라 하지 않았지만 검토할 만한 것.

| 날짜 | 항목 | 내용 |
|---|---|---|
| 2026-09-18 | 투영용 종류 상수의 위치 | `PUBLIC_NOTICE_KIND`와 `WHISPER_KIND`를 shared 상수로 옮기면 `NoticeView`·`WhisperView`의 `kind`를 유니온 타입으로 좁힐 수 있다. 화면이 kind로 분기하는 M4 전에 하면 된다 |
| 2026-09-18 | 단계 테스트 드라이버 중복 | M1 노트에서 이월. `test/engine/steps/` 파일들이 같은 드라이버를 복제하고 있다. 계획에 없는 파일이라 이번에도 추출하지 않았다 |

## 봇 자동 대전 결과 (2026-09-18)

`npm run sim:bots -w tal-brothers-server -- --games 300 --seed 1 --humans all`
구성마다 300판, 총 1200판. 같은 인자로 다시 돌리면 결과가 그대로 재현된다.

| 인간 | 평균 소요 | 타임오버 | 시계 0 도달 | Phase 3 도달 | Phase 2 종료 평균 잠식 | 배신자 평균 |
|---|---|---|---|---|---|---|
| 0명 | 52분 46초 | 0.0% | 87.7% | 100.0% | 77.7% | 0.00명 |
| 1명 | 26분 10초 | 0.0% | 10.3% | 100.0% | 80.9% | 0.00명 |
| 2명 | 31분 34초 | 0.0% | 14.0% | 81.7% | 73.8% | 0.78명 |
| 3명 | 35분 22초 | 0.0% | 21.0% | 87.7% | 76.5% | 1.13명 |

- 인간 0·1명 구성의 배신자 0명은 규칙대로다. 0명은 인간이 없고, 1명은 배신자 모드가 없다 (룰북 §10.1)
- 인간 0명 구성의 강제 잠식 87.7%는 Phase 3 A 루트의 유효표 0 때문이다 (확인 필요 항목 참조).
  시계 소모 상한 참고치로만 읽는다
- '시계 0 도달'은 Phase 3 투표 마감(= 시계 0)을 포함한다. Phase 1~2에서 끊긴 타임오버는 0%다

### 룰북 §19 조정 후보

| 관측 | 룰북의 예상 | 판단 |
|---|---|---|
| Phase 2 종료 시 평균 잠식 74~81%, 100% 좌석 23~34% | §19 주석 "50~70% 안착" | **초과.** 진입 환청 +20%와 환경 잠식 +40%가 겹친다는 §19 주석의 우려가 그대로 나타났다 |
| 배신자 판당 평균 1.13명(3인), 2명 이상 33% | §19 주석 "판마다 0~1명" | **초과.** 잠식 곡선을 낮추면 함께 내려간다 |
| 평균 소요 26~53분, 타임오버 0% | 게임 시계 100분 | **여유 과다.** 시계가 압박 장치로 작동하지 않는다 |
| 14A 성공률 0~10% | — | 봇은 제출하지 않고(§11) 인간도 그 시점에 부적이 드물어 저주가 거의 항상 터진다 |
| 변이 실제 비율 29.3 / 50.8 / 19.9 | 30 / 50 / 20 | 일치 |
| 개인 65~74% · 협동 74~77% · 비공개 62~65% · 대립 51~60% | — | 기준 4~6이 의도대로 작동한다 |

> 조정은 룰북 수정이므로 M2 범위 밖이다. 수치를 바꿀지, 바꾼다면 어느 값을 얼마나 바꿀지는 별도로 확인받는다.
