# M3 작업 노트

> `docs/m3-plan.md`를 실행하면서 생긴 기록을 모은다.
> 규칙의 원본은 `docs/rulebook-v3.md`이며, 이 문서는 구현 과정의 기록일 뿐 규칙을 정의하지 않는다.

## 착수 전 확정 (2026-09-19)

계획 10절의 해석 항목 15건이 모두 확정됐다. 결정 내용은 `docs/m3-plan.md` 10절 표에 있고,
문서에 반영한 곳은 다음과 같다.

| 대상 | 반영 내용 |
|---|---|
| `architecture.md` §5.1 | 액션이 두 종류 → **세 종류** (플레이어 명령, 타이머 만료, 연결 변화) |
| `architecture.md` §6 | 자동 저장을 **M5 예정**으로 표기. M3는 호출 자리만 비워 둔다 |
| `architecture.md` §7.3 | 좌석별 연결 상태 행 추가 (Display 전용, "연결 끊김" 표기) |
| `architecture.md` §8 | 자동 정지 한도를 넘기면 **이후로도** 자동 정지를 하지 않는다. 호스트 수동 정지는 계속 가능 |
| `architecture.md` §10 | 서버 인증을 `supabase.auth.getUser(token)`으로 확정 |
| `rulebook-v3.md` §17 | Display 공개 목록에 좌석별 연결 상태 추가 |
| `rulebook-v3.md` §21 | 좌석 연결 상태의 공개 범위 **(확정)** 행 추가 |
| `m3-plan.md` 7.1 | Vite 외부 노출(`server.host`)은 사람 작업이 아니라 **M3-7 구현**에 포함 |
| `m3-plan.md` 7.3 | 사람 작업에 **OAuth 동의 화면 구성(외부·테스트)** 추가, 번호 재정렬 (총 11건) |

## 변경 이력

이전 작업 단위에서 발견한 버그·설계 결함과 그 수정 내역.

| 날짜 | 작업 단위 | 내용 | 이유 |
|---|---|---|---|
| — | — | 없음 | |

## 계획 변경

`docs/m3-plan.md`와 달라진 결정.

| 날짜 | 항목 | 계획 | 실제 | 이유 |
|---|---|---|---|---|
| 2026-09-19 | QR 라이브러리 | `qrcode`(또는 동급) | `qrcode.vue` | 라이브러리 선택을 위임받았다. `qrcode`는 캔버스·Node 스트림까지 들고 오는 범용 라이브러리라 Vue에서 쓰려면 `ref`와 `onMounted`로 캔버스를 직접 그려야 한다. `qrcode.vue`는 Vue 3 SFC 컴포넌트 하나에 런타임 의존성이 없고 `<Qrcode :value="url" />` 한 줄로 끝난다. SVG로 렌더해 Display의 큰 화면에서 확대해도 깨지지 않는다 |
| 2026-09-19 | M3-1 | `session.hello`를 `Command` 유니온에 넣는다 | 넣지 않고 `protocol.ts`의 `HelloFrame`으로 둔다 | hello는 엔진 `dispatch`에 도달하지 않는 전송 계층 프레임이다 (계획 3.1의 프레임 표도 `command`와 별개의 `t`로 적었다). `Command`에 넣으면 엔진의 모든 분기가 절대 오지 않는 명령을 하나씩 더 처리해야 한다. `COMMAND_TYPE.SESSION_HELLO` 상수는 아키 §7.1 표기대로 남겨 둔다 |
| 2026-09-19 | M3-1 | 계획 M3-1 파일 목록에 없음 | `constants/protocolErrorCode.ts`와 `constants/apiErrorCode.ts`를 더 만들었다 | 계획 3.1의 `error` 메시지와 4.2의 REST 실패 응답이 둘 다 `code` 필드를 요구하는데 그 값 목록이 계획에 없었다. 클라이언트가 분기하는 값이라 문자열 리터럴로 흩뿌리지 않고 상수 파일로 고정했다. 파일명 ↔ export명 1:1 규칙을 따른다 |
| 2026-09-19 | M3-1 | `ACTION_KIND.PRESENCE` 추가가 M3-2 | 그대로 M3-2에 둔다 | 계획 M3-2 표대로다. `PresenceAction`을 유니온에 넣는 순간 `dispatch`의 명령 분기가 전부 함께 바뀌어야 해서 M3-1(= shared 범위, 서버는 컴파일만 유지)에서 떼어낼 수 없다 |
| 2026-09-19 | M3-1 | — | 투영 함수의 새 필드(`lobby`·`pause`·`seatConnections`·`connection`·`botTakeover`)를 중립값으로 채웠다 | 투영 **타입**은 M3-1, 그 값을 채우는 **상태 필드**는 M3-2다. M3-1 종료 조건이 server typecheck 통과라 한 단위 동안만 중립값을 두고 `// M3-2에서 상태를 읽는다` 표시를 남겼다 |
| 2026-09-19 | M3-2 | 계획 8.1의 `SeatState` 확장 필드는 `userId`·`connection`·`botTakeover` 3개 | `displayName`을 하나 더 두었다 | 계획 8.4의 `LobbyView`가 좌석별 `displayName`을 요구한다. 투영은 상태만 보고 만드는 순수 함수라 이름이 상태에 없으면 만들 수 없다. `userId`와 달리 화면에 그대로 나가는 값이다 |
| 2026-09-19 | M3-2 | 계획 5.2는 `nextDeadline`이 "둘 중 이른 시각"을 고른다고만 적었다 | `progress.stepTimerAt`(이 단계가 깨어날 시각)을 상태에 두고, `nextDeadline = min(stepTimerAt, 봇 대행 전환 시각)`으로 계산한다 | 타이머 액션에는 `(단계, 상태 버전)`만 실린다. 봇 대행 타이머가 먼저 발화했을 때 **단계 마감이 아직인지**를 알아야 단계 처리기를 부르지 않을 수 있는데, 중간 타이머(봇 부적 판단, 마감 1초 전)는 마감 시각으로 역산되지 않는다. 상태에 적어 두면 세이브·복원에서도 그대로 산다 |
| 2026-09-19 | M3-2 | `CommandAction.seat: BrotherRole` | `CommandAction.actor: ActionActor`(기기 역할 + 계정 + 좌석) | 로비 명령은 **좌석이 없는 Controller**가, 시작·봇 토글·정지는 **Display**가 보낸다. 좌석만으로는 보낸 사람을 가리킬 수 없다. ws 세션이 아는 값을 그대로 옮긴 모양이라 전송 계층과 1:1로 맞는다 |
| 2026-09-19 | M3-2 | 계획에 없음 | `StepHandler`에 `actorCommand`를 더했다 (로비 전용) | 대부분의 단계는 좌석 명령만 받는다. 그 7개 처리기의 시그니처를 모두 바꾸는 대신, 주체를 봐야 하는 단계만 별도 진입점을 쓴다. 좌석 해석과 봇 대행 거절은 dispatch가 한 곳에서 한다 |
| 2026-09-19 | M3-2 | 계획에 없음 | `host.pause`·`host.resume`·presence를 **단계 처리기보다 앞에서** 처리한다 | 정지·재개와 연결 변화는 특정 단계의 규칙이 아니라 방 운영이다. 단계마다 같은 처리를 반복하지 않고 dispatch 한 곳에 둔다 |
| 2026-09-19 | M3-2 | 계획 8.3은 시뮬레이터가 `lobby.start`를 한 번 보내면 된다고 적었다 | 시뮬레이터는 `startWithoutLobby`로 첫 이벤트에 바로 들어간다 | `lobby.start`는 사람이 앉은 좌석 1칸 이상을 요구한다(룰북 §1). 봇 자동 대전의 `--humans 0` 구성은 그 규칙 밖의 참고치라(룰북 §20) 로비를 지날 수 없다. 테스트 드라이버(`startGame`)는 계획대로 `lobby.start`를 지난다 |
| 2026-09-19 | M3-2 | — | `seatSetupForHumans`의 인간 좌석에 `local-<역할>` 가상 계정을 붙였다 | 로비 없이 만든 좌석은 `userId`가 비어 있어 "아무도 고르지 않은 좌석"이 되고, `lobby.start`가 봇으로 돌려 버린다. 테스트 드라이버도 손으로 적은 `{ isBot: false }` 좌석에 같은 계정을 채운다 |
| 2026-09-19 | M3-3 | 계획 8.4의 스냅샷은 Display용과 좌석용 둘 | `PublicSnapshot`(공개 항목만)을 하나 더 두었다 | 로비에서 **아직 좌석을 고르지 않은 Controller**가 받을 스냅샷이 없었다. Display 스냅샷을 주면 좌석별 연결 상태가 새고(룰북 §17), 좌석 스냅샷은 좌석이 있어야 만들 수 있다. 기존 `projectPublic`을 그대로 쓰므로 투영 함수는 늘지 않았다 |
| 2026-09-19 | M3-3 | 계획 2.1은 "방마다 운영용 `crypto` 구현 1개" | `engine/random.ts`에 `createCryptoRng`를 더했다 | 난수 인터페이스는 있었지만 운영 구현이 없었다(M1·M2는 시드 고정만 썼다). 아키 §5.5가 정한 자리에 둔다 |
| 2026-09-19 | M3-3 | 방 코드 길이·보관 기간을 `SERVER_ENV`에서 읽는다 (M3-5) | `roomRegistry`가 기본값(6자, 6시간)을 들고 `options`로 덮어쓸 수 있게 했다 | `SERVER_ENV`는 M3-5에서 만든다. M3-3에서 환경 변수에 의존하면 런타임 테스트가 환경을 타게 된다. M3-5가 값을 주입한다 |
| 2026-09-19 | M3-4 | 계획 9.1 M3-4 3번은 "깨진 JSON·모르는 type·필드 타입 불일치가 `unknownCommand`로 거절된다" | **깨진 JSON과 모르는 프레임 종류는 `error(badFrame)`**, 명령 본문이 어긋난 경우만 `rejected(unknownCommand)` | 계획 안에서 3.1과 9.1이 어긋났다. 3.1의 프로토콜 표가 "프레임을 못 읽었거나"를 `error`에 배정했으므로 그쪽을 따랐다. `rejected`는 `seq`를 실어 보내는데, 프레임 자체를 못 읽으면 `seq`가 없어 클라이언트가 어느 명령인지 짚을 수 없다. 프레임을 못 읽어도 **소켓은 닫지 않는다** — 계획 3.2가 닫으라고 적은 것은 인증 실패와 hello 타임아웃뿐이다 |
| 2026-09-19 | M3-4 | `infra/authVerifier.ts`는 M3-5 | 인터페이스(`AuthVerifier`·`AuthUser`)만 M3-4에서 먼저 만들었다 | ws 세션이 토큰 검증 포트를 필요로 한다. 구현(`supabase.auth.getUser`)은 계획대로 M3-5에서 붙인다 |
| 2026-09-19 | M3-4 | — | 연결 변화(presence)는 **엔딩에 도달한 뒤에도** 받는다 | `dispatch`는 `ENDING`에서 모든 액션을 `gameFinished`로 거절했다. 그러면 엔딩 화면을 보는 중에 소켓이 붙어도 스냅샷이 나가지 않는다. 연결 사실은 룰이 아니라 방 운영이라 엔딩 검사보다 앞에서 처리한다. 정지·봇 대행 판단은 종료 단계에서 스스로 걸러진다 |
| 2026-09-19 | M3-5 | 계획 M3-5 파일 목록에 없음 | `transport/http/httpApp.ts`(앱 조립), `httpErrors.ts`, `requireAuth.ts`를 나눠 만들었다 | `index.ts`는 포트를 연다. 그 파일에 앱 조립까지 넣으면 REST 테스트가 import만으로 서버를 띄우게 된다. 조립을 떼어 두면 테스트가 가짜 검증기·가짜 방 목록으로 앱만 만들 수 있다 (계획 9절) |
| 2026-09-19 | M3-5 | `serverEnv.ts` → `SERVER_ENV` (파일명 ↔ export명 1:1) | `loadServerEnv()` 함수와 `SERVER_ENV_DEFAULTS` 상수를 export한다 | 모듈 최상위 상수로 두면 **import하는 순간** 환경 변수를 읽어 테스트가 전부 환경을 타게 된다. 누락 시 즉시 실패(계획 7.2)는 `index.ts`가 `loadServerEnv()`를 부르는 시점에 일어난다 |
| 2026-09-19 | M3-5 | — | 서버 워크스페이스에 `@supabase/supabase-js`를 설치했다 | 계획 M3-5의 `infra/supabaseAdmin.ts`가 요구하는 의존성이다. web에만 있던 패키지를 서버에도 선언했다 (`npm install @supabase/supabase-js -w tal-brothers-server`) |
| 2026-09-19 | M3-5 | 계획 4.2의 참가 응답만 정의 | 참가 정원을 **3명**으로 두었다 | 룰북 §1의 "인간 플레이어 1~3명"이 상한이다. 좌석이 3칸이므로 멤버가 4명이 되면 누군가는 앉을 자리가 없다. 이미 멤버인 계정의 재참가는 그대로 통과시킨다(초대 링크를 두 번 열 수 있다) |
| 2026-09-19 | M3-5 | — | `tal-brothers-web/.env.example`도 함께 만들었다 | 계획 7.2 표가 web `.env.local` 키도 정의한다. 사람이 두 파일을 같은 방식으로 채울 수 있게 템플릿을 양쪽에 둔다 |
| 2026-09-19 | M3-6 | 계획 M3-6 파일 목록에 없음 | `src/config/webEnv.ts`를 만들었다 | Supabase 클라이언트와 REST·ws가 같은 환경 변수 4개를 읽는다. 세 파일에 흩어 놓는 대신 서버의 `serverEnv.ts`와 같은 자리를 둔다. 누락 시 읽는 순간 한국어로 실패한다 |
| 2026-09-19 | M3-6 | 계획 6.1은 로그인 후 복귀 경로를 "기억했다가" | `sessionStorage`에 남긴다 (`router/guards.ts`) | OAuth는 페이지를 떠났다 돌아오므로 메모리에 담아 두면 사라진다. 저장소가 막힌 환경(시크릿 모드)에서도 로그인 자체는 진행되도록 실패를 삼킨다 |

## 확인 필요

룰북에 없어 임의로 정할 수 없는 규칙·수치. 해당 부분에 의존하는 테스트는 `it.todo`로 남긴다.

| 날짜 | 항목 | 왜 필요한가 | 임시 처리 |
|---|---|---|---|
| 2026-09-19 | 호스트 수동 정지의 "이벤트 사이" | 아키 §8과 §7.1은 `host.pause`를 **이벤트 사이에만** 받는다고 적었지만, 그것이 어느 단계인지는 룰북·아키텍처 어디에도 없다 | **상황 제시(`EVENT_INTRO`) 단계에서만** 받도록 구현했다. 투표·굴림·개입 창은 입력 창이 열려 있어 "이벤트 사이"로 볼 수 없다. 결과 적용(`RESOLUTION`)은 타이머 없이 즉시 다음 이벤트로 넘어가 머무는 시점이 없다. 다른 해석(예: 어느 단계에서든 받고 다음 이벤트 경계에서 실제로 멈춘다)이면 `rules/pause.ts`의 `canHostPause` 한 곳만 고치면 된다 |

## 제안

범위 밖이라 하지 않았지만 검토할 만한 것.

| 날짜 | 항목 | 내용 | 상태 |
|---|---|---|---|
| 2026-09-19 | 브랜드 이미지 용량 | `introMask.png`가 7.7MB다. 빌드 결과의 대부분을 차지하고 폰에서 첫 화면이 느리다. 에셋 최적화는 M5 범위라 이번에는 건드리지 않았다 | 미결정 |
